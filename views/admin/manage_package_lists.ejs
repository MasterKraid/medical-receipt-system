<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Package Lists</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/admin_management.css">
</head>
<body>
    <div class="management-container">
        <header class="management-header">
            <h1>Manage Package Lists (Rate Databases)</h1>
            <a href="/admin-dashboard" class="back-link">Back to Dashboard</a>
        </header>

        <section class="section">
            <h2>Add New List</h2>
            <form action="/admin/package-lists/add" method="POST">
                <div class="form-grid" style="grid-template-columns: 2fr 1fr;">
                    <div>
                        <label for="name">List Name</label>
                        <input type="text" id="name" name="name" required placeholder="e.g., Corporate B2B Rates, Retail Walk-in">
                    </div>
                    <div style="align-self: end;">
                        <button type="submit" style="margin-top: 0;">Create List</button>
                    </div>
                </div>
            </form>
        </section>

        <section class="section">
            <h2>Existing Lists & Package Import</h2>
            <table class="data-table">
                <thead>
                    <tr>
                        <th>List Name</th>
                        <th style="width: 150px;">Package Count</th>
                        <th style="width: 50%;">Import Packages from Excel</th>
                    </tr>
                </thead>
                <tbody>
                    <% lists.forEach(list => { %>
                        <tr>
                            <td><strong><%= list.name %></strong></td>
                            <td><%= list.package_count %></td>
                            <td>
                                <form action="/admin/packages/upload" method="POST" enctype="multipart/form-data" class="upload-form">
                                    <input type="hidden" name="list_id" value="<%= list.id %>">
                                    <input type="file" name="package_file" accept=".xlsx, .xls" required style="width: 70%;">
                                    <button type="submit" class="action-btn" style="background-color: #28a745;">Upload</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                    <% if (lists.length === 0) { %>
                        <tr>
                            <td colspan="3" style="text-align: center;">No package lists found. Create one above to begin.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <div style="margin-top: 15px; padding: 10px; background-color: #e9ecef; border-radius: 5px; font-size: 0.9em;">
                <strong>Import Instructions:</strong> Your Excel file must have columns with the exact headers: <code>name</code>, <code>mrp</code>, and <code>b2b_price</code>. Any existing packages with the same name within a list will be updated with the new prices from the file.
            </div>
        </section>
    </div>
</body>
</html>