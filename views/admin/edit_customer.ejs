<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Customer: <%= customer.name %></title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/admin_management.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
</head>
<body>
    <div class="management-container">
        <header class="management-header">
            <h1>Edit Customer: <%= customer.name %></h1>
            <a href="/admin/customers" class="back-link">Back to Customers</a>
        </header>

        <form action="/admin/customers/edit/<%= customer.id %>" method="POST">
            <section class="section">
                <h2>Customer Details</h2>
                <div class="form-grid">
                    <div class="full-width">
                        <label for="name">Name</label>
                        <input type="text" id="name" name="name" value="<%= customer.name %>" required>
                    </div>
                    <div>
                        <label for="mobile">Mobile</label>
                        <input type="tel" id="mobile" name="mobile" value="<%= customer.mobile || '' %>">
                    </div>
                    <div>
                        <label for="dob">Date of Birth</label>
                        <input type="text" id="dob" name="dob" class="flatpickr-date" value="<%= customer.dob ? new Date(customer.dob).toLocaleDateString('en-GB') : '' %>" placeholder="DD/MM/YYYY">
                    </div>
                    <div>
                        <label for="age">Age</label>
                        <input type="number" id="age" name="age" value="<%= customer.age || '' %>" min="0" max="130">
                    </div>
                    <div>
                        <label>Gender</label>
                        <div class="radio-group">
                            <input type="radio" id="male" name="gender" value="Male" <%= customer.gender === 'Male' ? 'checked' : '' %>> <label for="male">Male</label>
                            <input type="radio" id="female" name="gender" value="Female" <%= customer.gender === 'Female' ? 'checked' : '' %>> <label for="female">Female</label>
                            <input type="radio" id="other" name="gender" value="Other" <%= customer.gender === 'Other' ? 'checked' : '' %>> <label for="other">Other</label>
                        </div>
                    </div>
                </div>
            </section>
            <button type="submit">Save Changes</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script>
        flatpickr(".flatpickr-date", { dateFormat: "d/m/Y", allowInput: true });
    </script>
</body>
</html>```
