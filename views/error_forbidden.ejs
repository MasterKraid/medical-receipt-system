<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .error-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px 40px;
            background: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 10px;
            text-align: center;
            border-top: 5px solid #dc3545;
        }
        .error-container h1 {
            color: #dc3545;
            font-size: 2em;
        }
        .error-container p {
            font-size: 1.1em;
            color: #333;
        }
        .error-container .actions {
            margin-top: 30px;
        }
        .error-container a {
            color: #007bff;
            text-decoration: none;
            margin: 0 10px;
        }
        .error-container a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>403 - Access Denied</h1>
        <p><%= message %></p>

        <% if (sessionUser) { %>
            <p>You are currently logged in as: <strong><%= sessionUser.username %></strong>.</p>
        <% } %>

        <div class="actions">
            <% if (sessionUser) { %>
                <a href="<%= sessionUser.isAdmin ? '/admin-dashboard' : '/dashboard' %>">Go to Your Dashboard</a>
            <% } else { %>
                 <a href="/">Go to Login</a>
            <% } %>
            <a href="javascript:history.back()">Go Back</a>
        </div>
    </div>
</body>
</html>